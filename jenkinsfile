pipeline {
    agent any

    stages {
        stage('addirion of no') {
            steps {
                script {
                    int a =1
                    int b=2
                    int sum =a+b 
                    echo = "sum of ${a} and ${b} is ${sum}"
                }
                
            }
        }
    }
}
